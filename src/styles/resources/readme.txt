To make the SCSS resource files in this folder available to component CSS files,
you must add each new file to `webpackConfig.sassResources` in webpack.config.js

Variables etc will then be available in your component's local CSS files.

Search for `/_typography` as an example.
